#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

void main(void)
{
   pid_t pid;
  
   pid = fork();
   if (pid > 0) {
     pid2 = fork();
     puts("Hello");
   }

  factorial(temp); 
}
